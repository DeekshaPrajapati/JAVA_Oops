package String_Intro;

public class StringObjectImmutable {
    /*
    Ques. What is String object immutable?
    Ques. Why String object is immutable?

    Immutable = not changable
    String object is immutable ,it means once string object is created its data or states can not be changed , but new string
    object is created.

    # Yadi hum string object ki value ko change karga to bo phle jo original value bhi rahgyi, and new object crete karke
    new value bhha put kar dega.
    Stirng s = new String("diks");
    Sop(S)   //O/p =   diks
   s= s.concat("java");
    Sop(S)   //O/p =   diks java

    # Why String Object are immutable?
    yadi mene 3 object crate kiya jisme reference var. different ha but unki value same he to "SCP" ka andar object yak
    banega and us object ko alg-2 reference variable point out karega. but yadi me kisi yak object ki value change kar du
    to all value per bhi effect pad sakta he , to
              iske liya "scp" area ke andar yak new object ban jyga and us object ka reference var. usko point out karne laga ga.
      isliya string object is immutable.
     */
}
