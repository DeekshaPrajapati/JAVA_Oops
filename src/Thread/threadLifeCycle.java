package Thread;

public class threadLifeCycle {
    /*
    1.create new thread
    2.thred are in runnable stage
    3.thred are running stage that means thread perform a task. here jvm allocate the processor.
    4. by default here thred again going on a  non-runnable stage, that means thred are in sleep mode.so they are going in runnable stage
    5. here thread task complete, so thred in a dead stage.
    After that thred are not follow a 1st step.
    and here user not invoke to again thred start they are occure a exception.
     */
}
